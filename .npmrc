engine-strict=true

# Never stop to ask before purging node_modules. The dev/preview containers run
# `pnpm i` with no one attached to answer, so a prompt hangs them instead of failing.
confirm-modules-purge=false
